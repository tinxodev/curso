import { log } from "console";

let firstName: string = "pepito";
let lastName: string = 'popi';

 let fullName: string = firstName +' '+ lastName;

 console.log(fullName);


//ejemplo de booleanos(true o false)

 let isActive: boolean = true;
 let isRunning: boolean = false;
 let hasKids: boolean = true;


 /*el simbolo de admiracion(!)puede cambiar el valor de  true y lo puede 
 cambiar a false EJ:
 isActive = !isActive
/*si quiero que vuelva a true sin borrar el simbolo de admiracion tengo que
colocar el mismo sibolo dos veces (!!)
isActive = !!isActive
sirve tanto en trues como en false*/

 console.log('isActive', isActive );
 console.log('isRuning', isRunning);
 console.log('hasKids', hasKids);
 

 























































